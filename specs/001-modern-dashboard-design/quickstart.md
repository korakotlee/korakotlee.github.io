# Quickstart

## Prerequisites
- Ruby (with Bundler)
- Jekyll

## Running Locally

1. Install dependencies:
   ```bash
   bundle install
   ```

2. Run the development server:
   ```bash
   bundle exec jekyll serve
   ```

3. Open browser:
   http://localhost:4000
